<div class="settings-page-heading">
    <div class="settings-page-heading__inner">
        <div>
            <p class="settings-page-heading__eyebrow">{{ __('Account controls') }}</p>
            <flux:heading size="xl" level="1" class="settings-page-heading__title">{{ __('Settings') }}</flux:heading>
            <flux:subheading size="lg" class="settings-page-heading__subheading">{{ __('Manage your profile and account settings') }}</flux:subheading>
        </div>

        <div class="settings-page-heading__chips">
            <span class="settings-status-chip settings-status-chip--info">{{ __('Profile') }}</span>
            <span class="settings-status-chip">{{ __('Security') }}</span>
        </div>
    </div>
</div>
